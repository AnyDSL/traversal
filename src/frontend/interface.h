/* interface.h : Impala interface file generated by impala */
#ifndef INTERFACE_H
#define INTERFACE_H

#ifdef __cplusplus
extern "C" {
#endif

struct BvhNode {
    float min[3];
    int child_tri;
    float max[3];
    int prim_count;
};

struct Accel {
    struct BvhNode* nodes;
    int* indices;
    float* vertices;
    int root;
};

struct Ray {
    float org[3];
    float dir[3];
    int tri;
    float tmin;
    float tmax;
    float u;
    float v;
};

void traverse_accel(struct Accel* accel_ptr, struct Ray* rays, int ray_count);

#ifdef __cplusplus
}
#endif

#endif /* INTERFACE_H */

